<h2>Τα βιβλία μου ({{books.length}})</h2>

{{#if books}}
<div class="table-responsive-sm">
  <table class="table table-hover">
    <thead>
      <tr>
        <th>Τίτλος</th>
        <th>Συγγραφέας</th>
        <th class="d-print-none">
          <a class="btn btn-sm btn-success" href='/addbookform'>Προσθήκη</a>
        </th>
      </tr>
    </thead>
    <tbody>
      {{#each books}}
        <tr>
          <td>{{this.title}}</td>
          <td>{{this.author}}</td>
          <td class="d-print-none">
            <a class="btn btn-sm btn-danger" href= '/deleteBook?title={{this.title}}'>Διαγραφή</a>
          </td>
        </tr>
      {{/each}}
    </tbody>
  </table>
</div>
{{/if}}
{{#unless books}}
<div class="row mt-5">
  <div class="col">
    Δεν υπάρχει κάποιο βιβλίο στο myBooks
  </div>
</div>
<div class="row mt-2">
  <div class="col">
    <a class="btn btn-sm btn-success" href='/addbookform'>Προσθήκη</a>
  </div>
</div>
{{/unless}}